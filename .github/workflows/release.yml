name: Release

# Publish the Tampermonkey userscript only when a version tag is pushed.
# main holds source; the `release` branch holds the stable install artifact.
#
# Security:
# - build/test: contents:read only, no persisted git credentials
# - publish: contents:write, but NO npm install; guard is a pre-bundled artifact
# - git credentials are injected only for the final push (not at checkout)
on:
  push:
    tags:
      - 'v*'

# Serialize publishes so an older tag cannot finish after a newer one and downgrade release.
concurrency:
  group: userscript-release
  cancel-in-progress: false

permissions: {}

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Ensure tag commit is on main
        run: |
          set -euo pipefail
          git fetch --no-tags origin main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "Refusing to release: $GITHUB_SHA is not an ancestor of origin/main"
            exit 1
          fi

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20

      - name: Install
        run: npm ci

      - name: Test
        run: npm test

      - name: Build userscript
        run: npm run build

      - name: Bundle release guard (semver inlined; publish job needs zero npm install)
        run: |
          set -euo pipefail
          mkdir -p dist
          npx esbuild scripts/release-guard.mjs \
            --bundle \
            --platform=node \
            --format=esm \
            --outfile=dist/release-guard.mjs
          test -f dist/release-guard.mjs
          # Smoke: first-publish path must allow
          node dist/release-guard.mjs "" "0.0.1" - package.json

      - name: Read package version
        id: pkg
        run: |
          VERSION=$(node -p "require('./package.json').version")
          TAG="${GITHUB_REF_NAME}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          if [ "${TAG}" != "v${VERSION}" ]; then
            echo "Tag ${TAG} does not match package.json version v${VERSION}"
            exit 1
          fi

      - name: Upload release artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: release-artifacts
          path: |
            userscript/ccswitch-linuxdo-importer.user.js
            dist/release-guard.mjs
          if-no-files-found: error

    outputs:
      version: ${{ steps.pkg.outputs.version }}
      tag: ${{ steps.pkg.outputs.tag }}

  publish:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      # No git credentials at checkout — token is only injected for the final push.
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Download release artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: release-artifacts
          path: .

      # Runtime only — no npm ci / no install scripts in the write-permission job.
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20

      - name: Publish userscript to release branch
        env:
          VERSION: ${{ needs.build.outputs.version }}
          TAG: ${{ needs.build.outputs.tag }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          ARTIFACT="userscript/ccswitch-linuxdo-importer.user.js"
          GUARD="dist/release-guard.mjs"
          test -f "$ARTIFACT"
          test -f "$GUARD"

          CURRENT_FILE=""
          CURRENT_VERSION=""
          if git fetch origin release:refs/remotes/origin/release 2>/dev/null; then
            CURRENT_FILE=$(mktemp)
            if git show origin/release:userscript/ccswitch-linuxdo-importer.user.js >"$CURRENT_FILE" 2>/dev/null; then
              CURRENT_VERSION=$(sed -n 's/^\/\/ @version[[:space:]]*//p' "$CURRENT_FILE" | head -n1 | tr -d '\r')
            else
              rm -f "$CURRENT_FILE"
              CURRENT_FILE=""
            fi
          fi

          if [ -n "${CURRENT_FILE}" ]; then
            node "$GUARD" "$CURRENT_VERSION" "$VERSION" "$CURRENT_FILE" "$ARTIFACT"
          else
            node "$GUARD" "" "$VERSION" - "$ARTIFACT"
          fi
          rm -f "${CURRENT_FILE:-}"

          WORKDIR=$(mktemp -d)
          if git show-ref --verify --quiet refs/remotes/origin/release; then
            git worktree add "$WORKDIR" origin/release
          else
            git worktree add --detach "$WORKDIR"
            git -C "$WORKDIR" checkout --orphan release
            git -C "$WORKDIR" rm -rf . >/dev/null 2>&1 || true
          fi
          mkdir -p "$WORKDIR/userscript"
          cp "$ARTIFACT" "$WORKDIR/userscript/ccswitch-linuxdo-importer.user.js"
          {
            echo '# CC Switch Importer — release artifacts'
            echo
            echo 'Stable Tampermonkey install target. Do not develop here.'
            echo
            echo '- Install: [ccswitch-linuxdo-importer.user.js](./userscript/ccswitch-linuxdo-importer.user.js)'
            echo '- Source: https://github.com/Super-YYQ/ccswitch-linuxdo-importer (main)'
            echo "- Version: ${VERSION}"
            echo "- Tag: ${TAG}"
          } > "$WORKDIR/README.md"
          git -C "$WORKDIR" config user.name "github-actions[bot]"
          git -C "$WORKDIR" config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git -C "$WORKDIR" add -A
          if git -C "$WORKDIR" diff --cached --quiet; then
            echo "No userscript changes to publish"
          else
            git -C "$WORKDIR" commit -m "release: userscript v${VERSION}"
            # Inject write credentials only for this push (checkout used persist-credentials: false).
            git -C "$WORKDIR" remote set-url origin \
              "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
            git -C "$WORKDIR" push origin HEAD:release
            # Drop token from remote URL after push.
            git -C "$WORKDIR" remote set-url origin \
              "https://github.com/${GITHUB_REPOSITORY}.git"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: ${{ needs.build.outputs.tag }}
          name: v${{ needs.build.outputs.version }}
          generate_release_notes: true
          files: userscript/ccswitch-linuxdo-importer.user.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
